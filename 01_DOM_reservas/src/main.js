document.addEventListener("DOMContentLoaded", () => {
    // Elemento de entrada a mi aplicación
    const app = document.getElementById("app");

    // Crear elemento h1
    const h1 = document.createElement("h1");
    h1.textContent = "Hello world";
    app.appendChild(h1);

    //app.appendChild(divCard);
});
